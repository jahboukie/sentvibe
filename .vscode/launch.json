{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CodeContext Pro Debug",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/../memory-extension"
      ],
      "outFiles": [
        "${workspaceFolder}/../memory-extension/out/**/*.js"
      ],
      "preLaunchTask": "npm: compile"
    }
  ]
}
